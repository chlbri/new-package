name: Experiment
on:
  schedule:
    - cron: '*/7 * * * *'

jobs:
  job1:
    name: Debug
    runs-on: ubuntu-latest

    steps:
      - name: Git checkout
        uses: actions/checkout@v4.1.6
        with:
          ref: debug
      - name: List files
        run: ls
