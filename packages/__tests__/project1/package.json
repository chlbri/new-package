{
  "name": "project1",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "posttest": "exit 0",
    "test:only": "vitest run --no-coverage",
    "_ci": "pnpm run test"
  },
  "devDependencies": {
    "@bemedev/core": "workspace:^",
    "typescript": "^6.0.3"
  }
}
